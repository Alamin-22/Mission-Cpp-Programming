#include <iostream>
using namespace std;

int main()
{

    int a = 7;
    // string result;
    // Ternary Operator is the same thing as if else just using the short hand technic just like the js
    /*
        result = (a % 2 == 0) ? "Even" : "Odd";

        cout << "The number is => " << result << endl; */

    // solving the same problem using if else

    if (a % 2 == 0)
    {
        cout << "The number is Even \n";
    }
    else
    {
        cout << "The number is Odd \n";
    }

    // solving leap year 

    



    return 0;
}